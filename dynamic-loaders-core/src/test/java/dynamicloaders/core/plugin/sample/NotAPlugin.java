package dynamicloaders.core.plugin.sample;

import dynamicloaders.core.plugin.PluginInfo;

public class NotAPlugin extends Object {

	protected static final PluginInfo info = new PluginInfo(
			"notAPlugin", "1.0.0", "Sample object that it's not a plugin"
	);

}
